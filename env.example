# Google Sheets API Credentials
# Option 1: Path to credentials JSON file
GOOGLE_CREDENTIALS_PATH=./key.json

# Option 2: Credentials as JSON string (alternative to file path)
# GOOGLE_CREDENTIALS_JSON={"type": "service_account", ...}

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Spreadsheet Configuration (optional - can also be set via API)
SPREADSHEET_ID=your_spreadsheet_id_here
SHEET_NAME=Sheet1

# Date Parsing Configuration (optional)
# Specify the starting cell for dates (e.g., C7 where dates begin)
DATE_START_CELL=C7
# Optional: Specify the start date explicitly (e.g., 24.11.2025)
# If not provided, will try to read from the cell itself
DATE_START=24.11.2025

